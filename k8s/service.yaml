apiVersion: v1
kind: Service
metadata:
  name: gpu-scheduling-toy
  labels:
    app: gpu-scheduling-toy
spec:
  selector:
    app: gpu-scheduling-toy
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP